# UserLilst
A simple Android application built using Kotlin and Jetpack Compose that fetches and displays a list of users from a public REST API. The app follows MVVM architecture, uses Retrofit with Coroutines for API calls, and demonstrates clean UI state handling.
